<%= form_for(type_partner) do |f| %>
  <% if type_partner.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(type_partner.errors.count, "error") %> prohibited this type_partner from being saved:</h2>

      <ul>
      <% type_partner.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :type %>
    <%= f.number_field :type %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_field :description %>
  </div>

  <div class="field">
    <%= f.label :documents_id %>
    <%= f.text_field :documents_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
